# Core Spark + streaming
pyspark==3.5.0
kafka-python==2.0.2

# Database
pymysql==1.1.0

# Data processing
numpy==1.24.3
pandas==2.0.3
# CRITICAL: pyarrow 14.x is required for pandas 2.0 + pyspark 3.5 Pandas UDFs
# Do NOT upgrade to 15+ without testing — Arrow IPC format changed
pyarrow==14.0.1

# Legacy ML (kept for train_model.py compatibility)
scikit-learn==1.3.2

# NLP utilities
nltk==3.8.1
vaderSentiment==3.3.2

# ── Transformer stack ──────────────────────────────────────────────────────────
# Install in this order: torch → tokenizers → transformers → sentencepiece
torch==2.1.2
tokenizers==0.15.1
transformers==4.37.2
sentencepiece==0.1.99

# Accelerate is optional but prevents a HuggingFace warning on CPU
# accelerate==0.27.2